enum genderEnum {
  MALE = "male",
  FEMALE = "female",
}

enum roleEnum {
  USER = "user",
  ADMIN = "admin",
}

enum providerEnum {
  LOCAL = "local",
  GOOGLE = " google",
}

export { genderEnum, roleEnum, providerEnum };
